# Dependencies

Normal orchestration uses installed DesireCore document/image access, team delegation and an available model. Resolve the current team and tool interface from the platform. Respect its actual access and approval rules.

Lead has no Python or Evidence interpreter prerequisite for natural-language dispatch. Each specialist chooses suitable installed tools and reports a capability gap only where it affects its work. A missing optional export validator does not block unrelated business analysis.

If the user requests a structured export, use the corresponding installed optional resources and their documented dependencies. State any unavailable export separately from the substantive findings. Platform/runtime dependencies retain their own licenses.
